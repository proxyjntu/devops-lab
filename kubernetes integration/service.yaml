apiVersion: v1
kind: Service
metadata:
  name: myflaskapp-service
spec:
  selector:
    app: myflaskapp
  type: NodePort   # Use LoadBalancer for cloud deployment
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
